<form method="post" action="{{ path('dossier_categorie_delete', {'id': dossier_categorie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dossier_categorie.id) }}">
    <button class="btn">Delete</button>
</form>
