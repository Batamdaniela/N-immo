{% extends 'base.html.twig' %}

{% block title %}OptionArticle{% endblock %}

{% block body %}
    <h1>OptionArticle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ option_article.id }}</td>
            </tr>
            <tr>
                <th>Valeur</th>
                <td>{{ option_article.valeur }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('option_article_index') }}">back to list</a>

    <a href="{{ path('option_article_edit', {'id': option_article.id}) }}">edit</a>

    {{ include('option_article/_delete_form.html.twig') }}
{% endblock %}
